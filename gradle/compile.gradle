sourceCompatibility = project.javaVersion
targetCompatibility = project.javaVersion

compileJava {
	options.encoding = project.defaultEncoding
	options.compilerArgs += ["-Xlint:-options"]
}

compileTestJava {
	options.encoding = project.defaultEncoding
	options.compilerArgs += ["-Xlint:-options"]
	classpath = files(jar.archiveFile) + configurations.testRuntimeClasspath
}

// Exclude test resources because there is huge number files and none of it is needed to be copied
processTestResources {
	exclude "**/*"
}